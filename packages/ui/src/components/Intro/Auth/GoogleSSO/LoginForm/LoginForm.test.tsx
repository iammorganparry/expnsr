describe('<LoginForm />', () => {
  
    it('should contain an input for email, password, login btn, forgot password link', () => {
        
    });
    
    it('should authenticate to supabase and return a webtoken', () => {
        
    });

    it('should fail gracefull and display error message', () => {
        
    });
})
